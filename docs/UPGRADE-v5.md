# v5 升级说明

已按用户于 2026-09-12 确认的方案实现：轻量内容底稿、主 Agent 自选视觉、制作与看图修订合并、整套最终审阅。独立 Layout 审阅、scaffold、固定 batch、强制子 Agent 和一次返修限制已退役。

图片上传／新增／替换／裁剪从 Layout 页迁移到实际页面审阅；区域与全局反馈统一进入制作。资产完整性、版本绑定、严格品牌复用与缺图导出保留。最终导出增加 PPTX 实际渲染检查状态。

工程验证：旧版 27 项 smoke 通过；候选版 20 项状态/反馈回归、6 项源素材/转换回归通过；隔离浏览器接口检查覆盖上传、撤销、裁剪、框选、真实提交、可编辑导出与渲染缓存；严格模板实例化和锁层校验通过。Frontmatter 和 Python 编译检查通过。

old/new 对照仅完成结构与工程行为对照：旧流程必须先模板选择和 Layout 冻结，新流程可直接从底稿制作；删除的批次、线框及派工门禁不再阻断。未完成同材料、同模型、干净上下文的真实质量/耗时对照，也未完成用户视觉验收。用户明确要求“改完就行，我自己换个地方做测验”，因此真实 Forward Test 和人工抽查留给用户，不能宣称视觉或性能提升已验证。evals/evals.json 提供五种测试 Prompt。

旧项目用已归档旧版继续完成，或从源材料在新目录开始；不混用状态机。旧版完整归档在本次工作区的 baseline/{codex,library,claude}，不进入活跃 Skill。
