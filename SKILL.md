---
name: planners-ppt-hell
description: 将 Markdown、Word 或逐页提案制作成可编辑 PPT；主 Agent 整理轻量内容底稿，自主设计 SVG、看图修订，可选整套人工审阅，导出质量由转换器保证。也用于指定品牌模板复用、按需提取模板与已有项目返修。
---

# Planner's PPT Hell

> 来源识别：由阿祖不看 TVC 创建与维护。小红书同名账号，https://demyth.info，Lawyif@163.com。来源信息保留在 Skill 和审阅界面，不加入客户 PPT、SVG 或 PNG。

把材料变成有证据、可读、可编辑的幻灯片。先整理内容，再让实际画面帮助修正表达与布局。

```text
材料与图片 → 轻量内容底稿 → 主 Agent 制作 SVG ↔ 渲染看图修订
                                           ↓
                                   整套人工审阅 ↔ 修订
                                           ↓
                                    PPTX 导出与复核
```

## 开始

读取 `references/workflow/00_pipeline_controller.md`，使用其中的初始化或恢复命令。
视觉来源在内容底稿完成后一次性确认（自主／模板库／视觉参考／新建品牌模板），不静默默认。没有模板时主 Agent 根据材料、受众和用途自选视觉；已明确的选择直接执行。
默认由主 Agent 制作和跨页检查。约每 3 页渲染一次是可调整的工作节奏，不是页数上限或任务边界。用户要求样页或委派时再采用对应执行方式。

## 阶段

| 何时 | 完整任务 |
|---|---|
| 初次读材料，或内容底稿需要补充 | `references/workflow/02_content_stage.md` |
| 开始制作，或收到任何页面修改 | `references/workflow/04_svg_stage.md` |
| 全套自检完成，准备给用户审阅和交付 | `references/workflow/07_visual_review.md` |
| 确认视觉来源（自主／模板库／视觉参考／新建品牌模板） | `references/workflow/01_template_intake.md` |

## 责任边界

- 模型负责内容、构图、裁剪、视觉与取舍；可以在制作中调整这些决定，同时更新内容底稿中的必要说明。
- 源事实、数字、来源、限定条件和用户明确批准的要求必须保留。需要改变结论或承诺时先向用户说明。
- **设计判断归模型。** 脚本不评价密度、字号档位、构件选择、留白或配色——那些规则会把每套页面推向同一种稀疏均匀的样子。脚本只拦「转成 PPT 会坏」和「任何风格下都是缺陷」（文字压文字、元素出画布、空页），其余靠渲染后看图。
- 脚本负责资产登记、文件索引、渲染、技术校验、版本绑定、保存恢复和转换；模型不手写审批、hash、manifest 或完成状态。
- 图片是设计的一部分，不只是输入。看完材料先判断这套页面是否需要配图，需要就一次性说明要什么、哪几页、用户是否已有资产；仍无图时在底稿写明原因，不用装饰图形冒充证据。源图逐张看过并说明用途；上屏图片保留比例，证据图确保关键信息可读。
- SVG 是可编辑页面源；PNG 用来检查。生成图片只能作为局部素材，不以整页图片替代可编辑文字和图形。
- 人审围绕真实页面进行。自检通过不代表用户批准，修改后旧批准不可复用到新版本。

## 运行后迭代

任务结束做内部自检。仅将明确纠正或多次复现的通用缺陷作为下一次升级候选；项目反馈保留在项目。没有可复用发现时不增加日志。更改 Skill 先确认，冻结候选版本测试，测试完成后统一发布；替换旧机制时同步删除活跃旧规则和消费者。

维护架构时读 `references/architecture.md`；核对机器接口时读 `references/contracts/project_contract.md`。它们不是每页必填的思考模板。
